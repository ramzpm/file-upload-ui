version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: build   # 👈 must match React build output
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
